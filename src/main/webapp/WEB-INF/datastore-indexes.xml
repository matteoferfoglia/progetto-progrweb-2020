<!-- File di configurazione per la definizione

     Fonte: https://cloud.google.com/datastore/docs/tools/indexconfig
     The Datastore emulator can generate the Datastore mode index configuration
     automatically as you test your application.
     Every Datastore mode query made by an application needs a corresponding index.
     Indexes for simple queries, such as queries over a single property, are created
     automatically. Indexes for complex queries must be defined in a configuration
     file named index.yaml. This file is uploaded with the application to create indexes
     in a Datastore mode database.
     The Datastore emulator automatically adds items to this file when the application
     tries to execute a query that needs an index that does not have an appropriate entry
     in the configuration file. You can adjust indexes or create new ones manually by
     editing the file. The index.yaml is located in the <project-directory>/WEB-INF/
     folder. By default, the data directory that contains
     WEB-INF/appengine-generated/index.yaml is ~/.config/gcloud/emulators/datastore/

     In questo progetto, gli indici compositi sono stati generati automaticamente
     dall'emulatore locale del Datastore e sono stati salvati in
        target/[nomeprogetto]/WEB-INF/appengine-generated/datastore-indexes-auto.xml
     ed il contenuto di tale file XML è stato copiato in questo file, così da tenere
     traccia degli indici (e non perderli a seguito del comando clean del progetto).
 -->

<datastore-indexes autoGenerate="true">

    <datastore-index kind="File" ancestor="false" source="auto">
        <property name="identificativoMittente" direction="asc"/>
        <property name="dataEdOraDiCaricamento" direction="asc"/>
    </datastore-index>

    <datastore-index kind="File" ancestor="false" source="auto">
        <property name="identificativoDestinatario" direction="asc"/>
        <property name="identificativoMittente" direction="asc"/>
        <property name="dataEdOraDiCaricamento" direction="desc"/>
    </datastore-index>

</datastore-indexes>