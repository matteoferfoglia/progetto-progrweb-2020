package it.units.progrweb.utils;

import org.junit.Assert;

/**
 * Classe di test per {@link RegexHelper}.
 * @author Matteo Ferfoglia
 */
public class RegexTest {

    // TODO

    // @ParameterizedTest // todo : fare il test
    //@CsvSource({
            // TODO : aggiungere un po' di email (coppie <email, isValida>)
    //})
    void isEmailValida(String email, boolean isValida) {
        
        // TODO : metodo da scrivere 
        //Assert.assertEquals(RegexHelper.isEmailValida(email), isValida);
        Assert.assertTrue(true);
    }


}
